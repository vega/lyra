'use strict';

var expect = require('chai').expect,
    enzyme = require('enzyme'),
    shallow = enzyme.shallow,
    mount = enzyme.mount,
    React = require('react'),
    configureMockStore = require('redux-mock-store'),
    DataPreview = require('./DataPreview');

describe('DataPreview component <DataPreview />', function() {
  
});
